0 Adds a script that prints Hello, World, followed by a new line to the standard output
1 Creer un script qui affiche un smiley confus "(Oo)"
2 Afficher le contenu du fichier /etc/passwd
3 Afficher le contenu de /etc/passwd et /etc/hosts
4 Afficher les 10 derni�res lignes de /etc/passwd
5 Afficher les 10 premi�res lignes de /etc/passwd
6 Ecrire un script qui affiche la troisième ligne du fichier iacta. Le fichier iacta sera dans le répertoire de travail
7 Ecrire un script shell qui crée un fichier nommé exactent \*\\'"Best School"\'\\*$\?\*\*\*\*\*:) contenant le texte Best School se terminant par une nouvelle ligne
8 Ecrire un script qui écrit dans le fichier ls_cwd_content le résultat de la commande ls -la. Si le fichier ls_cwd_content existe déjà, il doit être écrasé. Si le fichier ls_cwd_content n'existe pas, créez-le.
9 Ecrire un script qui duplique la dernière ligne du fichier iacta   Le fichier iacta sera dans le répertoire de ravail
10 Ecrire un script qui supprime tous les fichiers réguliers (pas les répertoires) avec une extension .js qui sont présents dans le répertoire actuel et tous ses sous-dossiers.
11 Ecrire un script qui compte le nombre de répertoires  de sous-répertoires dans le répertoire courant.Le répertoire courant et le répertoire parent ne doivent pas être pris en com. Les répertoires cachés doivent être comptés
12 Créez un script qui affiche les 10 fichiers les plus récents dans le répertoire actuel.   Exigences :  Un fichier par ligne  Classé du plus récent au plus ancien
13 Créez un script qui prend une liste de mots en entrée et n'imprime que les mots qui apparaissent exactement une foi  Format d'entrée : Une ligne, un mot   Format de sortie : Une ligne, un mot  Les mots doivent être triés
14 Afficher les lignes contenant le motif "root" dans le fichier /etc/passwd
15 Afficher le nombre de lignes contenant le motif "bin" dans le fichier /etc/passwd
16 Affiche les lignes contenant le motif "root" et les 3 lignes suivantes dans le fichier /etc/passwd.
17 Afficher toutes les lignes du fichier /etc/passwd qui ne contiennent pas le motif "bin"
18 Affiche toutes les lignes du fichier /etc/ssh/sshd_config commençant par une lettre  inclut également les lettres majuscules
19 Remplacer tous les caractères A et c de l'entrée par Z et e respectivement
20 Créer un script qui supprime toutes les lettres c et C de l'entrée
21 Ecrire un script qui inverse son entrée
22 Ecrire un script qui affiche tous les utilisateurs et leurs répertoires personnels, triés par utilisateurs.En se basant sur le fichier /etc/passwd
